DROP DATABASE IF EXISTS forum;
CREATE DATABASE forum CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE forum;

CREATE TABLE users (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL UNIQUE,
    email VARCHAR(150) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    role ENUM('admin', 'user') NOT NULL DEFAULT 'user',
    image VARCHAR(100) NOT NULL,
    bio TEXT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE posts (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(180) NOT NULL,
    content LONGTEXT NOT NULL,
    user_id INT UNSIGNED NOT NULL,
    image VARCHAR(100) NOT NULL, 
    published_at DATETIME NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_posts_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT
);

CREATE TABLE potd (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    post_id INT UNSIGNED NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE comments (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    post_id INT UNSIGNED NOT NULL,
    reply_id INT UNSIGNED NULL,
    content LONGTEXT NOT NULL,
    user_id INT UNSIGNED NOT NULL,
    published_at DATETIME NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE contacts (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL,
    email VARCHAR(150) NOT NULL,
    message TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO users (username, email, password, role, image, bio) VALUES
('admin', 'admin@admin.com', '$2y$10$FiXwJ.Y9GjRi0vZL6h331Ot56uIq9Apkr7zioO//gkv1uYDRamDBi', 'admin', '../vault/pfp1.png', 'admin ucet hehe'),
('magor', 'magor37@magor.com', '$2y$10$FiXwJ.Y9GjRi0vZL6h331Ot56uIq9Apkr7zioO//gkv1uYDRamDBi', 'user', '../vault/pfp2.png', 'trol');


INSERT INTO posts (title, content, user_id, image, published_at) VALUES
('ID request', 'I've seen this bug today, anyone have any idea what it is?', 2, '../vault/test1.png', '2026-03-20 10:00:00'),
('Favorite butterfly?', 'What is your favorite butterfly/moth OAT?',  1, '../vault/test2.png', '2026-03-20 10:00:00');

INSERT INTO potd (post_id, created_at) VALUES
(2, '2026-03-20 10:00:00');


INSERT INTO contacts (username, email, message) VALUES
('magor', 'magor37@magor.com', 'hehe');

INSERT INTO comments (post_id, reply_id, content, user_id,  published_at) VALUES
(1, NULL,'hehehehe', 1, '2026-03-20 10:00:00'),
(1, 1,'a to velmi', 2, '2026-03-20 10:00:00'),
(2, NULL,'a to velmi', 2, '2026-03-20 10:00:00');